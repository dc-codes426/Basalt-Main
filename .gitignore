CLAUDE.md
logs/

# Docker compose - only commit the example
docker-compose.yaml
docker-compose.*.yaml
!docker-compose.example.yaml

# Config - only commit the example
config.yaml
config.*.yaml
!config.example.yaml
