CLAUDE.md
logs/

# Docker compose - only commit the example
docker-compose.yaml
docker-compose.*.yaml
!docker-compose.example.yaml
!docker-compose.phala.yaml

# Config - only commit the example
config.yaml
config.*.yaml
config.example.yaml

# Environment files (secrets)
.env
.env.*
!.env.example

# Pre-launch script (contains deployment-specific logic)
prelaunchlocal.sh
inject_env.sh
update-images.sh

