services:
  vultiserver:
    image: basalt-vultiserver:latest
    container_name: basalt-vultiserver
    ports:
      - "8080:8080"
  redis:
    image: redis:latest
    container_name: basalt-redis
    ports:
      - "6379:6379"
    volumes:
      - redix_data:/data
volumes:
  redis_data:
